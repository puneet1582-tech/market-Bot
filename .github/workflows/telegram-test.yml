name: Telegram Test Bot

on:
  schedule:
    - cron: "30 3 * * *"

  

jobs:
  send-message:
    runs-on: ubuntu-latest
    steps:
      - name: Send message to Telegram
        run: |
          curl "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage?chat_id=${{ secrets.CHAT_ID }}&text=📊_Market_Update%0Aआज_NIFTY_और_BANKNIFTY_पर_नज़र_रखो%0ARisk_management_ज़रूरी_है"

